# Base URL of the FastAPI backend.
# Used by frontend/src/App.jsx as the API root for page-level requests.
# In `npm run dev`, requests to /api/* are additionally proxied to this host
# by the Vite dev server (see frontend/vite.config.js).
VITE_API_BASE_URL=http://127.0.0.1:8000
